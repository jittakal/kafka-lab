apiVersion: v1
kind: Service
metadata:
  name: {{ include "kafeventconsumer.fullname" . }}
  labels:
    {{- include "kafeventconsumer.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: metrics
  selector:
    {{- include "kafeventconsumer.selectorLabels" . | nindent 4 }}
